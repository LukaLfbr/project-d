{% extends 'base.html.twig' %}

{% block title %}
    Tasks Updater
{% endblock %}

{% block body %}
   {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.limit_date) }}
   {{ form_end(form) }}
 <a href="/" class="btn btn-danger">Retourner au menu <a/>
{% endblock %}
